import styled from "@emotion/styled"

const AboutEvent = () => {
  return <StyledAboutEvent></StyledAboutEvent>
}

const StyledAboutEvent = styled.section`
  width: 100%;
  height: 100vh;
  background: var(--bg);
`

export default AboutEvent
